var assert = chai.assert;


// Testing the Datagen get message array returns one with the right length
describe('Search param validation function', function() {
  it('Should return true false if a partial selection is made', function() {

    assert.equal(1 === 1);
  });
});
