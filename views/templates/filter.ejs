<div class="row">

    <select class="selectpicker"  title="Section" id="section-select">
        <option>No data</option>
    </select>


    <select class="selectpicker" multiple title="Category" id="category-select">
        <option>No data</option>
    </select>

    <select class="selectpicker" multiple title="Subsection" id="subsection-select">
        <option>No data</option>
    </select>

    <script>
        // Grab the new data array
        var sections =  JSON.parse('<%- sections %>');

        $('#section-select').append('<option>Test</option>').selectpicker('refresh');
        if(sections.length !== 0){
            $('#section-select').html(""); // Empty temp options
        }

        for(var i = 0; i < sections.length; i++){
            $('#section-select').append('<option>' + sections[i] + '</option>').selectpicker('refresh');
        }
        $(".selectpicker").selectpicker('refresh');

    </script>

</div>